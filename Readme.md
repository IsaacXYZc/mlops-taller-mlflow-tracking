# Taller MLflow Tracking

# Instrucciones
1. Instalar el entorno virtual de Python `venv\Scripts\activate`.
2. Instalar las dependencias con `pip install -r requirements.txt`.
3. Abrir la interfaz de MLflow con `mlflow ui`.
4. Ejecutar el notebook mlflow-tracking.ipynb.
